"""Gate T5: nonlinear collision radar.

The implementation is intentionally empty at this TDD step. Behavioral tests
will define the nonlinear recurrent compatibility experiment before the gate is
filled in.
"""
